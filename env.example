# Supabase Configuration
# Get these from your Supabase project: https://app.supabase.com
# Go to Project Settings > API

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (for server-side operations)
# Get this from Project Settings > API > service_role key (secret)
# This is used to bypass RLS for server-side operations
# IMPORTANT: Never expose this in client-side code!
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Google OAuth Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com
# 1. Create OAuth 2.0 credentials
# 2. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google (for dev)
# 3. Add authorized redirect URI: https://your-domain.com/api/auth/callback/google (for production)
# 4. Enable Google Drive API in your project

GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# NextAuth Configuration
# Generate a random secret: openssl rand -base64 32

AUTH_SECRET=your_nextauth_secret